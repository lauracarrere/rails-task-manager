<h1>My tasks</h1>

<ul class="list-group">
  <%@tasks.each do |task| %>
  <li class="list-group-item">
    <%if task.completed %>
        ☑︎ <%=link_to task.title, task_path(task) %><%=link_to "🗑", task_path(task), method: :delete %>
    <%else %>
        □ <%=link_to task.title, task_path(task) %><%=link_to "🗑", task_path(task), method: :delete %>
    <%end %>
  </li>
  <%end %>
</ul>

<%= link_to 'Add a new task', new_task_path, class: 'btn btn-success m-3 p-2'%>
